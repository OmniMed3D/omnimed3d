{
  "name": "@omnimed3d/inference-worker",
  "version": "0.1.0",
  "private": true,
  "description": "Client-side (browser) AI Inference Worker (REQ-A03/A09/A16) — model adapters, ONNX Runtime Web inference, mask postprocessing. Owned by the AI track (CODEOWNERS override within viewer/); model artifacts are produced offline by ai-pipeline/conversion and ai-pipeline/quantization.",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "typecheck:bench": "tsc --noEmit -p tsconfig.bench.json",
    "test": "vitest run",
    "bench": "vitest run test/latency-benchmark.test.ts",
    "bench:browser": "playwright test"
  },
  "dependencies": {
    "onnxruntime-web": "^1.19.2"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "@webgpu/types": "^0.1.72",
    "typescript": "^5.6.3",
    "vite": "^5.4.21",
    "vitest": "^2.1.4"
  }
}
